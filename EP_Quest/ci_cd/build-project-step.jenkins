pipeline {
    agent any
    options {
        buildDiscarder(logRotator(numToKeepStr: '4', artifactNumToKeepStr: '4'))
        timestamps()
    }
    stages {
        stage('First step') {
            steps {
                sh 'First step'
            }
        }
        stage('Second step') {
            steps {
                sh 'Second step'
            }
        }
    }
}